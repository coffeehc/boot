kind: pipeline
type: exec
name: default

platform:
  os: linux
  arch: amd64

steps:
  - name: greeting
    commands:
      - go version
  - name: build
    commands:
    - go build
    - go test
    when:
      branch:
      - master
      - feature/*
      event:
        - push

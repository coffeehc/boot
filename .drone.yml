kind: pipeline
type: exec
name: default

platform:
  os: linux
  arch: amd64

steps:
  - name: greeting
    commands:
      - echo hello world
  - name: build
    image: golang
    commands:
    - go build
    - go test
    when:
      branch:
      - master
      - feature/*
      event:
        - push
